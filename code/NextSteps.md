# Next Steps

- Add real audio assets for success/error cues and listen exercises under `code/assets/`.
- Replace the Speak stub with real audio capture (QAudioRecorder) and ASR integration.
- Expand the SQLite schema with migrations, indexes, and per-exercise history.
- Add richer tile ordering UI (drag/drop) instead of manual comma input.
- Increase unit test coverage for repositories and controller flows.
